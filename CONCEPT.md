# Воппер Чёрный Ниндзя — концепт курса

Курс для сотрудников ресторана: новый соус «Чёрный Ниндзя» (вкус месяца) +
стандарты сборки и упаковки Воппера.

**Стек.** Статика без сборки: один `index.html` (экраны — `.ku-page`, навигация
JS-роутером), `css/ninja.css`, `js/ninja.js`. Поверх дизайн-системы KU: токены
`--ku-*`, тёмная тема, рантайм `kuds.js` + `ku-scorm.js`. SCORM 1.2, офлайн.

## Структура

Обложка → **гл.1** видео о соусе → **гл.2** тест (5 вопросов) → **гл.3** два
тренажёра → результаты (+3 видео-разбора по желанию) → «Завершить курс».

## Гейтинг — последовательный, по факту выполнения (не по баллам)

| Открывается | Условие |
|---|---|
| Глава 2 | глава 1 просмотрена |
| Глава 3 | тест по соусу сдан (любой результат) |
| Тренажёр упаковки | сборка пройдена |
| «Завершить курс» | оба тренажёра пройдены |

Тест не блокирует: при ошибках даёт рекомендацию пересмотреть видео и кнопки
«Посмотреть видео повторно» / «Дальше».

## Интерактивы

**1. Тест по соусу.** 4 вопроса одиночного выбора + 1 на сопоставление
(3 пары «действие ↔ цель», `<select>`). Проверка по кнопке. После проверки:
разбор (верные — зелёным), буллиты «Коротко о главном», при неполном
результате — блок рекомендации.

**2. «Собери Воппер».** Перетаскивание + тап. 9 слоёв:
булочка → майонез → салат → томат → лук (щепотка) → соус Ниндзя → огурцы →
котлета → нижняя булочка. Ингредиент можно вставить **между** уже
добавленными (индикатор места вставки), поставленный слой — перетащить на
другое место или снять крестиком. Порядок засчитывается **в обе стороны**
(реальная сборка идёт снизу вверх). Два раунда: «Порядок» → «На время» (45 с).
Ошибка — **все** неверно стоящие слои краснеют + разбор рецепта; успех —
панель зеленеет, таймер замирает на «Готово за 0:XX».

**3. «Закрой и заверни».** Три шага на реальном макете обёрточной бумаги:

- **Поворот.** Клик поворачивает лист на 90°, верно — оранжевым лого к сборщику.
- **Посадка Воппера.** Перетаскивание только по оси Y: нижний край булочки
  должен лечь на печатную линию «Воппер» (32.07% высоты листа), верхняя треть
  при этом свисает за край бумаги.
- **Заворот.** Тянем нижний край листа вверх. Сгиб по линии `C` переносит точку
  `y → 2C − y`; верно, когда оранжевое лого (87.67% листа) совпало с центром
  Воппера. Лист лежит печатью вниз и просвечивает, поэтому база зеркальная
  (`scaleX(-1)`), а клапан при сгибе получает `rotate(180°)` и читается
  правильной стороной. Завёрнутая часть исчезает снизу (лист подрезается по
  сгибу), клапан полупрозрачный — видно, что лого встало по центру.

Задания не называют правильный ответ: при ошибке — подсказка, цель
подсвечивается только после успеха.

## Прогресс и завершение

`ku-scorm.js`: ключ `data-ku-course="whopper-ninja"`, упражнения помечены
`data-ku-id` (`sauce-test`, `assembly`, `packaging`), просмотры — `ch1-seen`,
`vid-*`. Прогресс пишется в `cmi.suspend_data` + `localStorage`, переживает
перезагрузку. Завершение — только кнопкой `[data-ku-complete]`. Объём
состояния ~175 символов из лимита 4096.

## Ассеты и ограничения

`assets/paper.png` — обёрточная бумага (макет с печатными линиями),
`assets/bun.png` — булочка вид сверху. Видео — `<iframe>` с внутреннего
bktube: вне корпоративной сети не проигрываются, структура и тренажёры при
этом работают.

**Отложено:** закрытие кламшелла и выкладка на БИН (нужны фото).
